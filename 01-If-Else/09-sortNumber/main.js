/*let num1 = +prompt("num1");
let num2 = Number(prompt("num2"));
let num3 = prompt("num3") *1;
let array = [num1,num2,num3];
array.sort((a,b)=>a-b) 
[-1, 0, 4] */

/*let a = prompt('num 1');
let b = prompt('num 2');
let c = prompt('num 3');
 
if (a >= b && b >= c ) {
    let max = a 
    let mid = b
    let min = c 
      console.log(`มาก ${max},กลาง ${mid}, น้อย ${min}`)
}
if (a >= c && c >= b ) {
    let max = a 
    let mid = c
    let min = b 
      console.log(`มาก ${max},กลาง ${mid}, น้อย ${min}`)
}
if (b >= a && a >= c ) {
    let max = b 
    let mid = a
    let min = c 
      console.log(`มาก ${max},กลาง ${mid}, น้อย ${min}`)
}
if (b >= c && c >= a ) {
    let max = b 
    let mid = c
    let min = a
      console.log(`มาก ${max},กลาง ${mid}, น้อย ${min}`)
}
if (c >= b && b >= a ) {
    let max = c 
    let mid = b
    let min = a
      console.log(`มาก ${max},กลาง ${mid}, น้อย ${min}`)
}
if (c >= a && a >= b ) {
    let max = c 
    let mid = a
    let min = b
      console.log(`มาก ${max},กลาง ${mid}, น้อย ${min}`)
} */